# ─────────────────────────────────────────────────────────────
# ⚠️ 安全红线：本仓库是 PUBLIC 公开仓库。
# 任何包含真实密钥、口令、个人隐私的文件都必须列在这里，
# 否则每周自动化的 `git add -A` 会把它推上 GitHub，
# 而 GitHub 有密钥扫描器 + 大量盗刷爬虫，明文 key 几分钟内就会被扫到并盗用。
# ─────────────────────────────────────────────────────────────

# 千问顾问页的内置默认 API Key（只存在于本地与腾讯云 CloudBase 镜像，绝不进 Git）
qwen-key.js

# 通用兜底
*.local.js
*.secret.js
.env
.env.*
!.env.example
